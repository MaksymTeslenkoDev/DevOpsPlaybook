# Notes

Basically containers are euphemeral entities, so it means that all sources are available and exists only while container is running. File system, networks, etc. 

Docker volumes can help solve the issue of data persistence. 

